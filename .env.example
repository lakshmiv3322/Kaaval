# Server-side Gemini API key (Required for AI-powered scam tactics classification)
GEMINI_API_KEY=

# Optional: Twilio credentials for real 3-way conference call barge-in (falls back to simulated bridge if empty)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
